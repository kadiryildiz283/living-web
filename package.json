{
  "name": "@living-web/sdk",
  "version": "1.0.0",
  "description": "Turn any website into a living world with autonomous, physics-aware browser characters",
  "main": "dist/index.js",
  "module": "dist/living-web.bundle.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "tsc --emitDeclarationOnly && esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm && esbuild src/index.ts --bundle --outfile=dist/living-web.bundle.js --format=esm && esbuild src/index.ts --bundle --outfile=dist/living-web.global.js --format=iife --global-name=LivingWeb",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "living-web",
    "browser-pet",
    "autonomous-agents",
    "game-loop",
    "physics-engine",
    "dom-scanner",
    "animation",
    "typescript"
  ],
  "author": "Kadir Yıldız <kadiryildiz283>",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.12.0",
    "playwright": "^1.62.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
